Questions: 

1. Seg fault is because of uninitialized pointers? 
2. Do we need to implement dropping the last 13 bits? Or does obj2bkeep already do that for us. in other words,
do we need to do anything else other than obj2bkeep when finding the superblock where an object comes from. NOPE
